# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Supabase Edge Function secrets — never commit real keys
supabase/functions/.env

# Local frontend env (use .env.local.example as the template)
.env

# Playwright output — generated on every run, never source
test-results/
playwright-report/

# Local verification scratch (archives, staging copies, quarantine folders)
_to_delete/
*.tgz
